NODE_ENV=test
PORT=4000

# Separate SQLite file from dev.db so tests never touch seeded demo data.
DATABASE_URL="file:./prisma/test.db"

JWT_SECRET=test-jwt-secret-not-for-real-use-at-least-16-chars
JWT_EXPIRES_IN=8h

UPLOAD_DIR=./uploads-test
MAX_UPLOAD_SIZE_MB=10
