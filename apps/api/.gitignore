node_modules

/src/generated/prisma

# Test database + upload dir (regenerated by `npm test`'s pretest migration step) — not needed
# to run the app itself, so kept out of version control unlike .env/dev.db/uploads/ above.
prisma/test.db
prisma/test.db-journal
uploads-test/
